# @package _global_

defaults:
  - _self_
  - data: ycb_standard # choose datamodule with `test_dataloader()` for evaluation
  - model: vit_module
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - callbacks: null
  - experiment: null

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
